const course = require("./lib/course"),
  file = require("./lib/file"),
  session = require("./lib/session"),
  folder = require("./lib/folder"),
  siding = require("./lib/siding")

module.exports = {
  course,
  file,
  session,
  folder,
  siding,
}
